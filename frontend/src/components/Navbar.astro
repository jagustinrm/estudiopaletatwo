<header class="w-full bg-primary text-white fixed top-0 left-0 shadow-md">
    <div class="max-w-7xl mx-auto px-6 flex justify-between items-center h-16">
      <!-- Logo -->
      <a href="#" class="text-xl font-bold">Mi Sitio</a>
  
      <!-- Menú -->
      <nav class="hidden md:flex gap-6">
        <a href="#" class="hover:text-gray-200">Inicio</a>
        <a href="#" class="hover:text-gray-200">Servicios</a>
        <a href="#" class="hover:text-gray-200">Contacto</a>
      </nav>
  
      <!-- Menú Móvil (Hamburguesa) -->
      <button class="md:hidden">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2"
          viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>
    </div>
  </header>
  
  <!-- Agregar padding al body para evitar que el contenido quede tapado -->
  <div class="h-16"></div>